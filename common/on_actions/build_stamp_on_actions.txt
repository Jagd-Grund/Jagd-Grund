on_actions = {

	on_startup = {
		effect = {
			# Build stamp -- forces the MP checksum to change on every release, even when a
			# balance edit lands only in files the game's checksum doesn't hash. Before each
			# push/export, bump the number below by 1. Safe no-op: flag is never read anywhere.
			set_global_flag = build_1
		}
	}
}
